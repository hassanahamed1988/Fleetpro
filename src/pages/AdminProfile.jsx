/* ============================================================================
   ADMIN PROFILE — placeholder. Structure only; content to be filled in later.
============================================================================ */

export default function AdminProfile() {
  return (
    <div className="flex flex-col gap-5">
      {/* TODO: Admin Profile content */}
    </div>
  );
}

