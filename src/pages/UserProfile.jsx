/* ============================================================================
   USER PROFILE — placeholder. Structure only; content to be filled in later.
============================================================================ */

export default function UserProfile() {
  return (
    <div className="flex flex-col gap-5">
      {/* TODO: User Profile content */}
    </div>
  );
}

